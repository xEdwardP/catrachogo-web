import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function AdminHomePage() {
  return (
    <PlaceholderScreen
      title="Panel de administración"
      description="Esta pantalla se construye en el Sprint 4 (panel de administración)."
    />
  );
}
